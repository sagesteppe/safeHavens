% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PosteriorCluster.R
\name{PosteriorCluster}
\alias{PosteriorCluster}
\title{Cluster environmental space using posterior beta draws}
\usage{
PosteriorCluster(
  model,
  predictors,
  f_rasts,
  pred_mat,
  training_data,
  n_draws = 100,
  n = 10,
  n_pts = 500,
  lyr = "occurrence_prob_mean",
  planar_proj,
  coord_wt = 2.5,
  consensus_method = c("hierarchical", "pam"),
  beta_draws = NULL,
  seed = 42
)
}
\arguments{
\item{model}{A \code{brmsfit} object from \code{\link[=bayesianSDM]{bayesianSDM()}}.}

\item{predictors}{A \code{SpatRaster} stack (\verb{$Predictors} from \code{\link[=bayesianSDM]{bayesianSDM()}}).}

\item{f_rasts}{The rasters output from \code{\link[=bayesianSDM]{bayesianSDM()}}, used to define the
spatial mask and domain. Specifically uses \verb{$RasterPredictions}.}

\item{pred_mat}{Data frame or matrix (\verb{$PredictMatrix} from \code{\link[=bayesianSDM]{bayesianSDM()}}).}

\item{training_data}{\code{sf} object (\verb{$TrainData} from \code{\link[=bayesianSDM]{bayesianSDM()}}).}

\item{n_draws}{Integer. Number of posterior beta draws to cluster over.
Defaults to \code{100}. Values between 50–200 give stable co-occurrence
matrices for typical SDM datasets (< 2000 obs, < 20 vars).}

\item{n}{Integer. Number of clusters per draw. Passed to
\code{\link[=EnvironmentalBasedSample]{EnvironmentalBasedSample()}} internals.}

\item{n_pts}{Integer. Points to sample per draw for clustering. Defaults to
\code{500}. The same points are used across all draws (fixed spatial frame)
so co-occurrence is directly comparable.}

\item{lyr}{Character. Layer name in \code{f_rasts} to use as the spatial domain
mask. Defaults to \code{"Threshold"} (the \code{\link[=PostProcessSDM]{PostProcessSDM()}} output
name).}

\item{planar_proj}{Numeric EPSG or proj4 string for planar projection.}

\item{coord_wt}{Numeric. Coordinate weight, as in \code{\link[=EnvironmentalBasedSample]{EnvironmentalBasedSample()}}.
Defaults to \code{2.5}.}

\item{consensus_method}{Character. How to derive the final clustering from
the co-occurrence matrix. One of:
\describe{
\item{\code{"hierarchical"}}{Average-linkage hierarchical clustering on
\code{(1 - co_occurrence)} as a dissimilarity. Stable and interpretable.
Default.}
\item{\code{"pam"}}{Partitioning Around Medoids on the dissimilarity matrix.
More robust to outliers than hierarchical.}
}}

\item{beta_draws}{Optional matrix of pre-drawn posterior samples (rows =
draws, cols = parameters, as from \code{\link[brms:draws-brms]{brms::as_draws_matrix()}}). If \code{NULL},
draws are taken internally. Supply this to reuse draws across multiple
calls.}

\item{seed}{Integer. Random seed. Defaults to \code{42}.}
}
\value{
A list:
\describe{
\item{\code{Geometry}}{\code{sf} polygon layer of consensus clusters (compatible
with \code{\link[=EnvironmentalBasedSample]{EnvironmentalBasedSample()}} \verb{$Geometry} output).}
\item{\code{CoOccurrenceMatrix}}{Numeric matrix (n_pts × n_pts). Each tupple
is the proportion of draws in which points i and j were assigned to the
same cluster.}
\item{\code{StabilityRaster}}{\code{SpatRaster} of per-cell cluster-assignment
stability, predicted via KNN regression from the sample point stability
scores. Values in (0,1); low values flag environmentally ambiguous
boundary regions.}
\item{\code{ConsensusRaster}}{\code{SpatRaster} of final consensus (rank1) cluster
IDs, geographically reordered.}
\item{\code{Rank2Raster}}{\code{SpatRaster} of second-most-frequent cluster
assignments across draws. Where a point was always assigned to one
cluster, rank2 = rank1.}
\item{\code{Rank3Raster}}{\code{SpatRaster} of third-most-frequent cluster
assignments. Where fewer than 3 unique clusters were assigned, rank3 =
rank1.}
\item{\code{Top3Lookup}}{Data frame with columns: \code{point_id}, \code{x}, \code{y},
\code{rank1_cluster}, \code{rank1_pct}, \code{rank2_cluster}, \code{rank2_pct},
\code{rank3_cluster}, \code{rank3_pct}, \code{uncertainty} (= 100 - rank1_pct).
Tabular summary of cluster assignment frequencies at sample points.}
\item{\code{DrawClusterings}}{Integer matrix (n_pts × n_draws) of per-draw
cluster assignments. Retained for diagnostics.}
\item{\code{SamplePoints}}{\code{sf} object of the fixed sample points used.}
\item{\code{KNN_Cluster}}{Trained KNN model for rank1 cluster assignment. Can
be reused to classify new points.}
\item{\code{KNN_Rank2}}{Trained KNN model for rank2 clusters.}
\item{\code{KNN_Rank3}}{Trained KNN model for rank3 clusters.}
\item{\code{KNN_Stability}}{Trained KNN regression model for stability scores.
Can be reused to predict stability at new locations.}
}
}
\description{
Implements option D of the Bayesian clustering workflow: draws K sets of
beta coefficients from the posterior, produces a weighted environmental
raster for each draw, samples points from each, and records how often pairs
of sample points are assigned to the same cluster. The resulting co-occurrence
probability matrix is then used to derive a consensus clustering.

Cells that co-cluster with probability near 1 across all draws are robustly
similar in environmental space. Cells near boundaries that shift across draws
— where the model is uncertain which variables matter most — will have
intermediate co-occurrence probability. This boundary-instability surface is
returned separately as \code{StabilityRaster} and is directly meaningful for
targeting collection effort.
}
\seealso{
\code{\link[=RescaleRasters_bayes]{RescaleRasters_bayes()}}, \code{\link[=EnvironmentalBasedSample]{EnvironmentalBasedSample()}},
\code{\link[=bayesianSDM]{bayesianSDM()}}
}
