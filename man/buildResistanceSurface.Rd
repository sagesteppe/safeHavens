% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildResistanceSurface.R
\name{buildResistanceSurface}
\alias{buildResistanceSurface}
\title{Create a simple, theoretical, raster surface modelling Isolation by Distance.}
\usage{
buildResistanceSurface(
  base_raster,
  resistance_surface = NULL,
  oceans = NULL,
  lakes = NULL,
  rivers = NULL,
  tri = NULL,
  habitat = NULL,
  w_ocean = 1000,
  w_lakes = 200,
  w_rivers = 20,
  w_tri = 1,
  w_habitat = 1
)
}
\arguments{
\item{base_raster}{SpatRaster. Base raster for the study area. Provides template geometry and resolution.}

\item{resistance_surface}{SpatRaster. Optional pre-computed resistance raster. If provided, the raster-building arguments are ignored.}

\item{oceans}{SpatRaster. Binary (0/1) raster for ocean cells. Used to increase movement cost.}

\item{lakes}{SpatRaster. Binary (0/1) raster for lakes.}

\item{rivers}{SpatRaster. Binary (0/1) raster for rivers.}

\item{tri}{SpatRaster. Continuous raster of topographic roughness (TRI). Used to increase cost in mountainous terrain.}

\item{habitat}{SpatRaster. Continuous raster of habitat suitability. Low values increase cost.}

\item{w_ocean}{Numeric. Weight applied to oceans (default 2000).}

\item{w_lakes}{Numeric. Weight applied to lakes (default 200).}

\item{w_rivers}{Numeric. Weight applied to rivers (default 20).}

\item{w_tri}{Numeric. Weight applied to TRI (default 1).}

\item{w_habitat}{Numeric. Weight applied to habitat suitability (default 1).}
}
\description{
Used in conjunction, preferably before \code{populationResistance} in the \code{IBRBasedSample} workflow.
}
\examples{
\dontrun{
# Prepare resistance raster

# this also can run internally in `population resistance`,
# but for time sakes is best to prep ahead of time
# especially if treating multiple species in the same domain.
res <- buildResistanceSurface(
  base_raster = base_rast,
  oceans = ocean_r,
  lakes = lakes_r,
  rivers = rivers_r,
  tri = tri_r
)
}
}
