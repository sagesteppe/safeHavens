% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BayesianSDM.R
\name{compute_aoa_bayes}
\alias{compute_aoa_bayes}
\title{Compute Area of Applicability for Bayesian SDM}
\usage{
compute_aoa_bayes(
  model,
  train,
  predictors,
  cv_folds,
  newdata = NULL,
  use_posterior_mean = TRUE,
  LPD = TRUE,
  maxLPD = 1,
  ...
)
}
\arguments{
\item{newdata}{SpatRaster of predictors for which to compute AOA. Typically
this is \code{sdm_result$Predictors}.}

\item{use_posterior_mean}{Logical. If \code{TRUE} (default), weights variables
by posterior mean coefficient values. If \code{FALSE}, all variables weighted
equally.}

\item{LPD}{Logical. Compute Local Point Density? Defaults to \code{TRUE}.}

\item{maxLPD}{Numeric or integer. Number of nearest neighbors for LPD.
See \code{\link[CAST:aoa]{CAST::aoa()}}. Defaults to \code{1} (use all training data).}

\item{...}{Additional arguments passed to \code{\link[CAST:aoa]{CAST::aoa()}}.}

\item{sdm_result}{List output from \code{\link[=bayesianSDM]{bayesianSDM()}}. Must contain \code{Model},
\code{TrainData}, \code{Predictors}, and \code{CVStructure}.}
}
\value{
An object of class \code{aoa} from CAST. See \code{\link[CAST:aoa]{CAST::aoa()}} for details.
}
\description{
Computes the Area of Applicability (AOA) for a Bayesian SDM fitted with
\code{\link[=bayesianSDM]{bayesianSDM()}}. This is a wrapper around \code{\link[CAST:aoa]{CAST::aoa()}} that extracts the
necessary components from the brmsfit object and formats them for CAST.
}
\details{
The function extracts environmental predictor names from the brmsfit object,
computes variable weights from posterior mean coefficients (if
\code{use_posterior_mean = TRUE}), and formats the spatial CV folds from CAST
knndm for the AOA calculation.
}
