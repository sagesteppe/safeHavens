% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PredictiveProvenance.R
\name{rescaleFuture}
\alias{rescaleFuture}
\title{Rescale future climate predictors using current model coefficients}
\usage{
rescaleFuture(
  model,
  future_predictors,
  current_predictors,
  training_data,
  pred_mat
)
}
\arguments{
\item{model}{glmnet model object from \code{elasticSDM_noPCNM()$Model}.}

\item{future_predictors}{SpatRaster of future climate variables.  Names must
match those retained in \code{model}.}

\item{current_predictors}{SpatRaster of current climate (provides the
standardisation parameters).}

\item{training_data}{the same data that went into the glmnet model, this is used
for calculating variance which is required for the scaling process. From \code{elasticSDM}}

\item{pred_mat}{the Prediction matrix from \code{elasticSDM}}
}
\value{
SpatRaster with rescaled future predictors (one layer per
non-zero coefficient).
}
\description{
Standardises future predictors using the mean and SD from \emph{current} climate,
then applies the glmnet beta weights.  This is the same weighting applied
upstream in \code{RescaleRasters} — here we just do it against a different
raster stack.
}
