% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BayesianSDM.R
\name{bayesianSDM}
\alias{bayesianSDM}
\title{Fit a Bayesian spatial GLMM as a species distribution model}
\usage{
bayesianSDM(
  x,
  predictors,
  planar_projection,
  quantile_v = 0.025,
  prior_type = c("horseshoe", "normal", "student"),
  prior_scale = 1,
  pca_predictors = TRUE,
  pca_axes = 5,
  gp_scale_prior = NULL,
  resample = FALSE,
  feature_selection = c("ffs", "none"),
  vif = TRUE,
  min_ffs_var = 5,
  chains = 4,
  iter = 5000,
  warmup = 2000,
  cores = 4,
  k = 5,
  seed = 42,
  backend = "cmdstanr",
  fact = 3,
  ...
)
}
\arguments{
\item{x}{An \code{sf} object of occurrence points. Must have an \code{occurrence}
column coded 0/1, OR the function will assume all rows are presences and
generate pseudo-absences (matching \code{\link[=elasticSDM]{elasticSDM()}} behaviour).}

\item{predictors}{A \code{terra} SpatRaster stack of environmental predictors.}

\item{planar_projection}{Numeric EPSG code or proj4 string for a planar
(metre-unit) CRS. Used for spatial thinning and to derive GP coordinates.
See \code{\link[=elasticSDM]{elasticSDM()}} for guidance.}

\item{quantile_v}{Numeric thinning quantile passed to \code{\link[spThin:thin]{spThin::thin()}}.
Defaults to \code{0.025}. Set to \code{0.001} for essentially no thinning.}

\item{prior_type}{Character. Prior family placed on the fixed-effect
environmental coefficients. One of:
\describe{
\item{\code{"horseshoe"}}{Regularised horseshoe (Piironen & Vehtari 2017).
Best when many predictors may be irrelevant. Requires the expected
number of non-zero coefficients via \code{p0} (see \code{...}).}
\item{\code{"normal"}}{\code{Normal(0, prior_scale)}. Simple and fast; reasonable
when predictors are pre-selected or are PCA axes.}
\item{\code{"student"}}{Student-t(3, 0, prior_scale). Heavier tails than
Normal; a middle ground.}
}
Defaults to \code{"horseshoe"}.}

\item{prior_scale}{Numeric. Scale parameter for \code{"normal"} and \code{"student"}
priors. Ignored for \code{"horseshoe"}. Defaults to \code{1} (weakly informative on
the log-odds scale when predictors are standardised).}

\item{pca_predictors}{Logical. If \code{TRUE}, replace the raw predictor stack
with its principal components before fitting. The number of axes retained
is controlled by \code{pca_axes}. Defaults to \code{FALSE}.}

\item{pca_axes}{Integer. Number of PCA axes to retain when
\code{pca_predictors = TRUE}. Defaults to \code{5}.}

\item{gp_scale_prior}{A \code{brms} prior object for the GP length-scale
parameter, or \code{NULL} to use brms default. The default \code{NULL} uses
\code{inv_gamma(3, 1)}, which is weakly informative and appropriate for most
ecological datasets. Override only if you have strong prior knowledge about
the spatial range of your species (e.g., \code{prior(inv_gamma(5, 2), class = lscale, coef = "")}
for tighter spatial autocorrelation).}

\item{resample}{Boolean, Defaults to FALSE. Used to place 15\% of the requested points in areas undersampled by sdm::background functions.}

\item{feature_selection}{Character. Variable selection method to apply before
Bayesian model fitting. One of:
\describe{
\item{\code{"ffs"}}{Forward feature selection via CAST::ffs(). Uses spatial CV
folds to select variables. Fast and spatially-aware. Default.}
\item{\code{"none"}}{No feature selection; use all predictors (or all PCA axes).
Relies on horseshoe prior for shrinkage.}
}}

\item{vif}{Boolean, default TRUE, Whether to run usdm::vifcor to remove highly collinear features, default theta used.}

\item{min_ffs_var}{Integer. Minium number of ffs vars to start with.}

\item{chains}{Integer. Number of MCMC chains. Defaults to \code{4}.}

\item{iter}{Integer. Total iterations per chain (including warmup). Defaults
to \code{2000}.}

\item{warmup}{Integer. Warmup iterations per chain. Defaults to \code{1000}.}

\item{cores}{Integer. Parallel cores. Defaults to \code{parallel::detectCores()}.}

\item{k}{Integer. Number of spatial CV folds (CAST \code{knndm}). Defaults to
\code{5}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42}.}

\item{backend}{Character. Stan backend passed to \code{\link[brms:brm]{brms::brm()}}. One of
\code{"cmdstanr"} (recommended, faster) or \code{"rstan"}. Defaults to
\code{"cmdstanr"}.}

\item{fact}{Numeric, default 2.0.
Factor to multiple the number of occurrence records by to generate the number of background (absence) points. #'}

\item{...}{Additional arguments forwarded to \code{\link[brms:brm]{brms::brm()}}. Also accepts
\code{p0} (integer, expected non-zero predictors) for the horseshoe prior;
defaults to \code{floor(ncol(pred_matrix) / 2)}.}
}
\value{
A named list with the following elements, mirroring \code{\link[=elasticSDM]{elasticSDM()}}:
\describe{
\item{\code{RasterPredictions}}{\code{SpatRaster} of posterior mean predicted
occurrence probability.}
\item{\code{RasterPredictions_sd}}{\code{SpatRaster} of posterior SD (uncertainty).}
\item{\code{Predictors}}{The predictor raster stack actually used (may be PCA
axes if \code{pca_predictors = TRUE}).}
\item{\code{PCNM}}{Always \code{NULL}; slot retained for workflow compatibility.}
\item{\code{Model}}{The fitted \code{brmsfit} object.}
\item{\code{CVStructure}}{CAST \code{knndm} object with fold indices.}
\item{\code{LOO}}{\code{loo} object from approximate leave-one-out cross-validation
via PSIS-LOO. Replaces \code{ConfusionMatrix}.}
\item{\code{ConfusionMatrix}}{A simple threshold-based confusion matrix
(threshold = 0.5 on posterior mean) on the held-out test fold, for
comparability with \code{\link[=elasticSDM]{elasticSDM()}} output.}
\item{\code{TrainData}}{\code{sf} object used for model fitting.}
\item{\code{TestData}}{\code{sf} object held out for evaluation.}
\item{\code{PredictMatrix}}{The numeric matrix of predictor values used for
the spatial raster prediction.}
\item{\code{PCAModel}}{The \code{prcomp} object if \code{pca_predictors = TRUE},
otherwise \code{NULL}. Retained so users can project new data onto the same
axes.}
\item{\code{Diagnostics}}{Named list: \code{Rhat} (max R-hat across all parameters),
\code{BulkESS} (min bulk ESS), \code{TailESS} (min tail ESS). Flags convergence
problems automatically.}
}
}
\description{
A Bayesian alternative to \code{\link[=elasticSDM]{elasticSDM()}} using \code{brms}. Fits a binomial GLMM
with a Gaussian process spatial random effect over the full pairwise distance
matrix (via a kernel covariance, no mesh approximation). All environmental
predictors are retained; shrinkage toward zero is handled by priors rather
than variable selection.

Users should either (a) remove highly correlated predictors beforehand, or
(b) pass PCA axes as predictors (see \code{pca_predictors} argument), which is
the recommended workflow.

The function returns an object structurally compatible with the \code{safeHavens}
downstream workflow: the same named list slots are populated where
meaningful, with Bayesian-specific additions (posterior draws, LOO-CV).
}
\examples{
\dontrun{
library(sf)
library(terra)

x <- read.csv(file.path(system.file(package = "dismo"), "ex", "bradypus.csv"))
x <- sf::st_as_sf(x[, c("lon", "lat")], coords = c("lon", "lat"), crs = 4326)

files <- list.files(
  path    = file.path(system.file(package = "dismo"), "ex"),
  pattern = "grd",
  full.names = TRUE
)
predictors <- terra::rast(files)

# Recommended workflow: PCA axes as predictors, horseshoe prior
result <- bayesianSDM(
  x                 = x,
  predictors        = predictors,
  planar_projection = 5070,
  pca_predictors    = TRUE,
  pca_axes          = 5,
  prior_type        = "horseshoe",
  chains            = 4,
  iter              = 2000
)

terra::plot(result$RasterPredictions)
terra::plot(result$RasterPredictions_sd)  # uncertainty surface
print(result$Diagnostics)
}

}
\seealso{
\code{\link[=elasticSDM]{elasticSDM()}} for the elastic-net alternative,
\code{\link[brms:brm]{brms::brm()}}, \code{\link[CAST:knndm]{CAST::knndm()}}
}
