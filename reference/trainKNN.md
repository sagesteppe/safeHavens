# partition data and train a simple KNN model

Simply use this to partition your test data and quickly train a simple
model

## Usage

``` r
trainKNN(x, split_prop)
```

## Arguments

- x:

  the weighted matrix including a class ID in the column 'ID'

- split_prop:

  prop for the data partitions.
