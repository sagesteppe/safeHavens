# Recursively grab a named component of a list.

Recursively grab a named component of a list.

## Usage

``` r
get_elements(x, element)
```

## Arguments

- x:

  a list of lists

- element:

  the quoted name of the list element to extract.
